# Config Directory

Reserved for future configuration files.

Phase 0 does not add runnable configuration. Future real-control configuration must default to dry-run and require explicit safety switches.
